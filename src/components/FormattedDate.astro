---
const { date } = Astro.props;
---

<time datetime={date.toISOString()} class="text-sm font-semi-bold">
  {
    date.toLocaleDateString("sv-se", {
      year: "numeric",
      month: "numeric",
      day: "numeric",
    })
  }
</time>
